### Archivo de ejemplo para variables de entorno en producción
# No incluyas credenciales privadas en el repositorio real.

# URL pública del backend (sin trailing slash), por ejemplo:
VITE_API_URL=https://inventario-backend.onrender.com

# Ruta base para la API si tu backend expone /api/v1
VITE_API_V1=/api/v1

# Otras variables opcionales (p. ej. claves de analytics) pueden ir aquí
# VITE_SENTRY_DSN=
