# Example workflow kept for reference. Moved to root CI.
# To re-enable, move this file back to .github/workflows/ci.yml at repo root.
name: CI (example)

on:
  workflow_dispatch:

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Example only"
