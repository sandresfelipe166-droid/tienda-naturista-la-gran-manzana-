# Configuración de desarrollo para pruebas desde móviles (LAN)
ENVIRONMENT=development
# IP local de tu PC (ipconfig -> IPv4)
LOCAL_DEV_IP=192.168.1.50
# Puerto del cliente (Vite)
DEV_CLIENT_PORT=5173
# Permitir cualquier Host header en desarrollo (útil cuando accedes por IP)
ALLOW_ALL_HOSTS_DEV=true
# Si prefieres controlar orígenes CORS manualmente, descomenta y ajusta:
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://192.168.1.50:5173
